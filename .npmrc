# Use a flat (hoisted) node_modules during the JS→TS migration so module
# resolution matches the pre-workspace npm behavior. Tighten to pnpm's default
# isolated linking in the Phase 5 cleanup once deps are declared per-package.
node-linker=hoisted
