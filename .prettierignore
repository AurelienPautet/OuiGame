node_modules
dist
apps/web/dist
.turbo
Public
pnpm-lock.yaml
**/migrations/**
*.sql

# Generated by TypeScript / Vitest — not source, keep Prettier off them.
*.tsbuildinfo
coverage

# claude-code-action validates these workflow files are byte-identical to the
# version on the default branch (a secrets-exfiltration guard), so reformatting
# them breaks its run. Keep them out of Prettier's reach.
.github/workflows/claude.yml
.github/workflows/claude-code-review.yml
