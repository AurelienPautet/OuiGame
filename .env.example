# Copy this file to `.env` and fill in any blanks.
# The defaults below match the local Docker Postgres dev setup (see
# database_setup.txt). `.env` itself is gitignored — never commit real secrets.

# Express / Socket.IO backend port (Vite dev client runs separately on :5173)
PORT=8000

# Postgres — local dev runs in Docker on host port 5433
DB_HOST=localhost
DB_PORT=5433
DB_USER=ouigame
DB_PASSWORD=ouigame
DB_NAME=ouigame

# Google OAuth client ID — from Google Cloud Console → APIs & Services → Credentials
GOOGLE_CLIENT_ID=

# Password-reset emails (Resend). Without RESEND_API_KEY the reset flow still
# works end-to-end but the email is skipped (logged to the console instead) —
# fine for local dev. Get a key at https://resend.com.
RESEND_API_KEY=
# "From" address for transactional email. Must be on a domain verified in
# Resend; defaults to Resend's shared onboarding domain if unset.
EMAIL_FROM=OuiTank <onboarding@resend.dev>
# Public base URL of the web client, used to build the password-reset link
# (the client uses a HashRouter, so the path is appended after '#'). Local dev:
# the Vite client runs on :5173.
WEB_URL=http://localhost:5173
